/// <reference types="Cypress" />

// Faz um apelido , similar ao SQL, para um elemento então a busca por ele já fica mapeada
// pode ser acessado através do prefixo @  ex: as('apelido')   get('@apelido')

it ("cy.as() - Selecionando Elementos", () => {
    cy.as()
})