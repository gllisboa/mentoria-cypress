/// <reference types="Cypress" />

//Permite que o usuário realize uma busca nos elementos pelo texto do html, o que aprece na tela
// Se for precedido de um get() a busca vai ser realizada no escopo do elmento retornado pelo get

it ("cy.contains() - Selecionando Elementos", () => {
    cy.contains()
})